package matematica.basico.multiplicacaodivisaointeiro;

// Divisão de inteiros em contexto: uma grandeza total negativa de magnitude $t é repartida
// igualmente entre $n partes: resultado = -(t / n).
public enum TipoIntDiv
{
	REPARTIR_NEGATIVO
}
