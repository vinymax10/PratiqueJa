package matematica.basico.adicaonatural;

// Variantes estruturais do problema de soma contextualizada.
// Por ora há uma só (pergunta o total). O enum mantém o padrão e deixa
// espaço para variantes futuras (ex.: PARTE, onde se pergunta uma das parcelas).
public enum TipoSomaContexto
{
	TOTAL
}
